package tasks.lesson3;

public class ReverseString {
    public static String reverseString(String s) {
        // нужно записать String задом-наперед
        // идея заключается в превращение String в массив (тип char - символы)
        // далее мы создаем новый массив и записываем в него чары из первого в обратном порядке
        // в конце мы возвращаем String созданный из этого массива (для этого у String есть нужный конструктор):
        // new String(charArray)

        return null;
    }
}
